services:
  web:
    build:
      context: .
    ports:
      - 127.0.0.1:8000:8000
    develop:
      watch:
        - action: sync
          path: .
          target: .
          ignore:
            - static/
        - action: rebuild
          path: pyproject.toml
        - action: rebuild
          path: /static/
